// Optional JavaScript functionality
document.addEventListener("DOMContentLoaded", function() {
    // Example: Add a simple log when the page is loaded
    console.log("Page loaded successfully!");

    // You can add interactivity here as needed (e.g., dynamic content, event handling, etc.)
});
